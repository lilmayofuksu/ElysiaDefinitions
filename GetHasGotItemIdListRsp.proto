syntax = "proto2";

message GetHasGotItemIdListRsp {
    required Retcode retcode = 1;
    repeated uint32 item_id_list = 2;

    enum CmdId {
        CMD_ID = 105;
    }
    enum Retcode {
        SUCC = 0;
        FAIL = 1;
    }
}