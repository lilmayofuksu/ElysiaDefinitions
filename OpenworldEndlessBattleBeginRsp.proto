syntax = "proto2";

message OpenworldEndlessBattleBeginRsp {
    required Retcode retcode = 1;

    enum CmdId {
        CMD_ID = 3439;
    }
    enum Retcode {
        SUCC = 0;
        FAIL = 1;
    }
}