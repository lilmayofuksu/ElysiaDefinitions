syntax = "proto2";

message SealedModeStartSelectionRsp {
    required Retcode retcode = 1;

    enum CmdId {
        CMD_ID = 599;
    }
    enum Retcode {
        SUCC = 0;
        FAIL = 1;
        IS_SELECTING = 2;
        AVATAR_FULL = 3;
    }
}