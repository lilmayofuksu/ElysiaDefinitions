syntax = "proto2";

message OpenworldChapterActiveTalentRsp {
    required Retcode retcode = 1;

    enum CmdId {
        CMD_ID = 4664;
    }
    enum Retcode {
        SUCC = 0;
        FAIL = 1;
        MAP_ERROR = 2;
        TALENT_ERROR = 3;
        BUILDING_LOCKED = 4;
        UNIQUE_TAG_REPEATED = 5;
    }
}