syntax = "proto2";

message ModifyScDLCNameRsp {
    required Retcode retcode = 1;

    enum CmdId {
        CMD_ID = 6327;
    }
    enum Retcode {
        SUCC = 0;
        FAIL = 1;
        FORMAT_ERROR = 2;
        SENSITIVE_WORDS = 3;
    }
}