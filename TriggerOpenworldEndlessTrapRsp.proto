syntax = "proto2";

message TriggerOpenworldEndlessTrapRsp {
    required Retcode retcode = 1;

    enum CmdId {
        CMD_ID = 3429;
    }
    enum Retcode {
        SUCC = 0;
        FAIL = 1;
        NOT_JOIN = 2;
    }
}