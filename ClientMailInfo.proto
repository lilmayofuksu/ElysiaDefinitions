syntax = "proto2";

import "ClientMailStyleInfo.proto";
message ClientMailInfo {
    optional uint32 total_num = 1;
    optional uint32 can_fast_delete_num = 2;
    repeated ClientMailStyleInfo mail_style_info_list = 3;
}