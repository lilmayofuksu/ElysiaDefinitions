syntax = "proto2";

message EndlessSingleModeActivityTakeItemRsp {
    required Retcode retcode = 1;

    enum CmdId {
        CMD_ID = 4047;
    }
    enum Retcode {
        SUCC = 0;
        FAIL = 1;
        ITEM_FULL = 2;
    }
}